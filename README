Various stuff that I like to use.

Run setup.rb to set up all the links and everything.  Roughly it runs this:

git submodule init
git submodule update
ln -s emacs.d/ .emacs.d

ln -s tmux/tmux.conf .tmux.conf
